{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/bin/hello",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "CMake: Build"
    }
  ]
}
